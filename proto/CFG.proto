syntax = "proto3";

package com.stripe.rubytyper;
import "proto/Symbol.proto";

message BasicBlock {
    int32 id = 1;
}

message CFG {
    Symbol symbol = 1;
    repeated BasicBlock basic_blocks = 2;
}
